<!DOCTYPE html>
<html>
<head>
  <title>BlogImport</title>
  

<script src="http://vkontakte.ru/js/api/openapi.js" type="text/javascript"> VK.init(function()
{
  //Задаём параметры через "&" которые передаются в рефере, это нужно в том
  //случае если вы не будете хранить параметры в ссылках при переходе в iframe приложении
  VK.loadParams(vk_location_href);

  VK.api
  (
    'isAppUser',
    function(response)
    {
      if(response.response == 0)
      {
         //Отслеживаем когда выполнится добавление приложения
         VK.addCallback
         (
           'onApplicationAdded',
           function()
           {
          
           }
         );

         //Выводим окно с просьбой установить приложение
         VK.callMethod('showInstallBox');
      }
    }
  );
});</script>

  <script src="http://vkontakte.ru/js/api/xd_connection.js?2" type="text/javascript"></script>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>

</head>
<body>
   <script type="text/javascript">

    VK.init({
    apiId: 2451953
    });

    VK.UI.button('login_button');

    VK.callMethod('showInstallBox',0);
    </script>

<%= current_user %>
<%= params.inspect %>
<%= yield %>

</body>
</html>
